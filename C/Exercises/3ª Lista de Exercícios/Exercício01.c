#include <stdio.h>
#include <stdlib.h>

int main()
{
    int num, quadrado, raiz;
    printf("Digite um número: ");
    scanf("%d", &num);

    if(num > 0){
        quadrado = num * num;
        raiz = 
    }
}